{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    },
    {
      "src": "backend/package.json",
      "use": "@vercel/node"
    }
  ],
  "scripts": {
    "build": "npm install && cd backend && npm install && npm run build",
    "dev": "npm install && cd backend && npm install && npm run dev"
  }
}